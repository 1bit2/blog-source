+++
date = '2026-04-16'
title = 'Linux 上下文切换实现原理深度分析'
weight = 5
tags = [
    "上下文切换",
    "schedule",
    "__schedule",
    "switch_to",
    "CFS",
]
categories = [
    "进程",
]
+++
# Linux 上下文切换实现原理深度分析

> 基于 Linux 5.15.78 内核源码分析

---

## 目录

- [一、核心概念区分](#一核心概念区分)
- [二、上下文切换整体流程](#二上下文切换整体流程)
- [三、核心数据结构](#三核心数据结构)
- [四、调度入口分析](#四调度入口分析)
- [五、上下文切换核心实现](#五上下文切换核心实现)
- [六、地址空间切换](#六地址空间切换)
- [七、寄存器切换（汇编层）](#七寄存器切换汇编层)
- [八、硬件上下文切换（C层）](#八硬件上下文切换c层)
- [九、上下文信息保存位置](#九上下文信息保存位置)
- [十、系统调用与上下文切换的区别](#十系统调用与上下文切换的区别)
- [十一、总结](#十一总结)

---

## 一、核心概念区分

### 1.1 模式切换 vs 上下文切换

| 概念 | 模式切换 (Mode Switch) | 上下文切换 (Context Switch) |
|------|------------------------|----------------------------|
| **定义** | 用户态 ↔ 内核态 | 进程A ↔ 进程B |
| **是否换进程** | ❌ 同一个进程 | ✅ 不同进程 |
| **切换什么** | 特权级别 (Ring 3 → Ring 0) | 整个执行环境 |
| **触发场景** | 系统调用、中断、异常 | 调用 schedule() 且选择不同进程 |
| **开销** | 较小 | 较大（涉及页表、TLB等） |

### 1.2 图解对比

```
┌────────────────────────────────────────────────────────────────────────┐
│                           模式切换 (系统调用)                           │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  进程A用户态 ──────→ 进程A内核态 ──────→ 进程A用户态                    │
│                        始终是进程A                                      │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────────────┐
│                           上下文切换 (进程切换)                         │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  进程A内核态 ─────────────────→ 进程B内核态                             │
│                  换了一个进程！                                         │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 二、上下文切换整体流程

### 2.1 流程图

```
schedule()
    │
    ▼
__schedule()
    │
    ├── 1. 获取当前CPU运行队列 (rq)
    ├── 2. 选择下一个任务 (调度器: CFS/RT/DL)
    │
    ▼
context_switch(prev, next)
    │
    ├── prepare_task_switch()        # 准备工作
    │
    ├── switch_mm_irqs_off()         # 切换地址空间 (页表)
    │       │
    │       └── load_new_mm_cr3()    # 写CR3寄存器
    │
    ├── switch_to(prev, next)        # 寄存器切换
    │       │
    │       └── __switch_to_asm()    # 汇编实现
    │               │
    │               ├── 保存 callee-saved 寄存器
    │               ├── 切换栈指针 (RSP)
    │               ├── 恢复 callee-saved 寄存器
    │               │
    │               └── __switch_to()   # C函数
    │                       │
    │                       ├── FPU 状态切换
    │                       ├── 段寄存器切换
    │                       ├── TLS 加载
    │                       └── 更新 per-CPU current
    │
    └── finish_task_switch()         # 清理工作
```

### 2.2 调用链

```
schedule()
  └── __schedule()
        └── context_switch()
              ├── switch_mm_irqs_off()     [地址空间切换]
              ├── switch_to()               [宏]
              │     └── __switch_to_asm()   [汇编]
              │           └── __switch_to() [C函数]
              └── finish_task_switch()
```

---

## 三、核心数据结构

### 3.1 task_struct（进程描述符）

每个进程都有一个 `task_struct`，其中包含了进程的所有信息：

```c
// 文件: include/linux/sched.h

struct task_struct {
    // ... 其他字段 ...
    
    struct mm_struct *mm;           // 进程地址空间
    struct mm_struct *active_mm;    // 活动地址空间（内核线程借用）
    
    void *stack;                    // 指向内核栈
    
    struct thread_struct thread;    // 硬件上下文（架构相关）
    
    // ... 其他字段 ...
};
```

### 3.2 thread_struct（硬件线程上下文）

保存进程的硬件上下文，在上下文切换时保存/恢复：

```c
// 文件: arch/x86/include/asm/processor.h

struct thread_struct {
    /* TLS 描述符 */
    struct desc_struct  tls_array[GDT_ENTRY_TLS_ENTRIES];
    
    /* 内核栈指针 - 上下文切换的核心！ */
    unsigned long       sp;
    
#ifdef CONFIG_X86_64
    /* 段寄存器 */
    unsigned short      es;
    unsigned short      ds;
    unsigned short      fsindex;
    unsigned short      gsindex;
    
    /* FS/GS 基址 */
    unsigned long       fsbase;
    unsigned long       gsbase;
#endif

    /* 调试寄存器 */
    unsigned long       virtual_dr6;
    unsigned long       ptrace_dr7;
    
    /* 异常信息 */
    unsigned long       cr2;
    unsigned long       trap_nr;
    unsigned long       error_code;
    
    /* I/O 权限 */
    struct io_bitmap    *io_bitmap;
    unsigned long       iopl_emul;
    
    /* 内存保护键 */
    u32                 pkru;
    
    /* FPU/SSE/AVX 状态 - 必须在最后，大小可变 */
    struct fpu          fpu;
};
```

### 3.3 pt_regs（用户态寄存器帧）

进入内核时保存用户态寄存器，位于内核栈顶：

```c
// 文件: arch/x86/include/asm/ptrace.h (x86-64)

struct pt_regs {
    /* Callee-preserved 寄存器 */
    unsigned long r15;
    unsigned long r14;
    unsigned long r13;
    unsigned long r12;
    unsigned long bp;
    unsigned long bx;
    
    /* Callee-clobbered 寄存器 - 进入内核时保存 */
    unsigned long r11;
    unsigned long r10;
    unsigned long r9;
    unsigned long r8;
    unsigned long ax;
    unsigned long cx;
    unsigned long dx;
    unsigned long si;
    unsigned long di;
    
    /* 系统调用号 / 错误码 / IRQ号 */
    unsigned long orig_ax;
    
    /* iretq 返回帧 */
    unsigned long ip;       // 用户程序执行位置
    unsigned long cs;
    unsigned long flags;    // RFLAGS
    unsigned long sp;       // 用户栈指针
    unsigned long ss;
};
```

### 3.4 inactive_task_frame（上下文切换帧）

上下文切换时保存在栈上的寄存器帧：

```c
// 文件: arch/x86/include/asm/switch_to.h

struct inactive_task_frame {
#ifdef CONFIG_X86_64
    unsigned long r15;
    unsigned long r14;
    unsigned long r13;
    unsigned long r12;
#else
    unsigned long flags;
    unsigned long si;
    unsigned long di;
#endif
    unsigned long bx;
    
    /* 栈帧头，用于栈回溯 */
    unsigned long bp;
    unsigned long ret_addr;  // 切换后的返回地址
};
```

### 3.5 struct fpu（FPU/SIMD 状态）

```c
// 文件: arch/x86/include/asm/fpu/types.h

struct fpu {
    /*
     * 记录上次在哪个CPU加载过FPU寄存器
     * 用于懒惰恢复优化
     */
    unsigned int            last_cpu;
    
    /* AVX512 使用时间戳 */
    unsigned long           avx512_timestamp;
    
    /*
     * FPU 寄存器的内存副本
     * 上下文切换时保存/恢复
     */
    union fpregs_state      state;
};
```

---

## 四、调度入口分析

### 4.1 schedule() 函数

```c
// 文件: kernel/sched/core.c

asmlinkage __visible void __sched schedule(void)
{
    struct task_struct *tsk = current;

    // 提交异步工作
    sched_submit_work(tsk);
    
    do {
        // 禁用抢占
        preempt_disable();
        
        // 核心调度函数
        __schedule(SM_NONE);
        
        // 重新启用抢占
        sched_preempt_enable_no_resched();
    } while (need_resched());  // 循环直到不再需要重新调度
    
    sched_update_worker(tsk);
}
EXPORT_SYMBOL(schedule);
```

### 4.2 __schedule() 函数

```c
// 文件: kernel/sched/core.c

static void __sched notrace __schedule(unsigned int sched_mode)
{
    struct task_struct *prev, *next;
    unsigned long *switch_count;
    unsigned long prev_state;
    struct rq_flags rf;
    struct rq *rq;
    int cpu;

    // 获取当前 CPU 和运行队列
    cpu = smp_processor_id();
    rq = cpu_rq(cpu);
    prev = rq->curr;  // 当前正在运行的任务

    schedule_debug(prev, !!sched_mode);

    // 禁用本地中断
    local_irq_disable();
    rcu_note_context_switch(!!sched_mode);

    // 获取运行队列锁
    rq_lock(rq, &rf);
    smp_mb__after_spinlock();

    // 更新运行队列时钟
    rq->clock_update_flags <<= 1;
    update_rq_clock(rq);

    switch_count = &prev->nivcsw;
    
    // 读取进程状态
    prev_state = READ_ONCE(prev->__state);
    
    // 如果进程不是可运行状态，从队列中移除
    if (!(sched_mode & SM_MASK_PREEMPT) && prev_state) {
        if (signal_pending_state(prev_state, prev)) {
            WRITE_ONCE(prev->__state, TASK_RUNNING);
        } else {
            prev->sched_contributes_to_load =
                (prev_state & TASK_UNINTERRUPTIBLE) &&
                !(prev_state & TASK_NOLOAD) &&
                !(prev->flags & PF_FROZEN);

            deactivate_task(rq, prev, DEQUEUE_SLEEP | DEQUEUE_NOCLOCK);
            // ...
        }
        switch_count = &prev->nvcsw;
    }

    // 选择下一个要运行的任务
    next = pick_next_task(rq, prev, &rf);
    clear_tsk_need_resched(prev);
    clear_preempt_need_resched();

    // 如果选择了不同的任务，执行上下文切换
    if (likely(prev != next)) {
        rq->nr_switches++;
        
        // 更新 rq->curr
        RCU_INIT_POINTER(rq->curr, next);
        
        ++*switch_count;

        migrate_disable_switch(rq, prev);
        psi_sched_switch(prev, next, !task_on_rq_queued(prev));

        trace_sched_switch(sched_mode & SM_MASK_PREEMPT, prev, next);

        // ★★★ 执行上下文切换 ★★★
        rq = context_switch(rq, prev, next, &rf);
    } else {
        // 没有切换，释放锁
        rq->clock_update_flags &= ~(RQCF_ACT_SKIP|RQCF_REQ_SKIP);
        rq_unpin_lock(rq, &rf);
        __balance_callbacks(rq);
        raw_spin_rq_unlock_irq(rq);
    }
}
```

---

## 五、上下文切换核心实现

### 5.1 context_switch() 函数

```c
// 文件: kernel/sched/core.c

static __always_inline struct rq *
context_switch(struct rq *rq, struct task_struct *prev,
               struct task_struct *next, struct rq_flags *rf)
{
    // 准备切换
    prepare_task_switch(rq, prev, next);

    // 通知架构开始切换（用于虚拟化）
    arch_start_context_switch(prev);

    /*
     * 地址空间切换逻辑：
     * 
     * kernel -> kernel   lazy + transfer active
     *   user -> kernel   lazy + mmgrab() active
     *
     * kernel ->   user   switch + mmdrop() active
     *   user ->   user   switch
     */
    if (!next->mm) {
        // 切换到内核线程
        enter_lazy_tlb(prev->active_mm, next);
        next->active_mm = prev->active_mm;
        if (prev->mm)
            mmgrab(prev->active_mm);
        else
            prev->active_mm = NULL;
    } else {
        // 切换到用户进程
        membarrier_switch_mm(rq, prev->active_mm, next->mm);
        
        // ★ 切换页表（地址空间）★
        switch_mm_irqs_off(prev->active_mm, next->mm, next);

        if (!prev->mm) {
            rq->prev_mm = prev->active_mm;
            prev->active_mm = NULL;
        }
    }

    rq->clock_update_flags &= ~(RQCF_ACT_SKIP|RQCF_REQ_SKIP);

    prepare_lock_switch(rq, next, rf);

    // ★★★ 切换寄存器和栈 ★★★
    switch_to(prev, next, prev);
    barrier();

    // 完成切换（此时已经是新进程在执行了！）
    return finish_task_switch(prev);
}
```

### 5.2 prepare_task_switch() 函数

```c
// 文件: kernel/sched/core.c

static inline void
prepare_task_switch(struct rq *rq, struct task_struct *prev,
                    struct task_struct *next)
{
    kcov_prepare_switch(prev);
    sched_info_switch(rq, prev, next);
    perf_event_task_sched_out(prev, next);
    rseq_preempt(prev);
    fire_sched_out_preempt_notifiers(prev, next);
    kmap_local_sched_out();
    prepare_task(next);
    prepare_arch_switch(next);
}
```

### 5.3 finish_task_switch() 函数

```c
// 文件: kernel/sched/core.c

static struct rq *finish_task_switch(struct task_struct *prev)
    __releases(rq->lock)
{
    struct rq *rq = this_rq();
    struct mm_struct *mm = rq->prev_mm;
    long prev_state;

    // 注意：此时 prev 可能在其他 CPU 上运行或已经被释放
    // 除了读取 prev->state，不应该再访问 prev 的其他字段

    vtime_task_switch(prev);
    perf_event_task_sched_in(prev, current);
    finish_task(prev);
    tick_nohz_task_switch();
    finish_lock_switch(rq);
    finish_arch_post_lock_switch();
    kcov_finish_switch(current);
    kmap_local_sched_in();

    fire_sched_in_preempt_notifiers(current);
    
    // 释放之前借用的 mm（如果有）
    if (mm) {
        membarrier_mm_sync_core_before_usermode(mm);
        mmdrop(mm);
    }

    // 如果前一个进程已经死亡，释放其资源
    if (unlikely(prev_state == TASK_DEAD)) {
        if (prev->sched_class->task_dead)
            prev->sched_class->task_dead(prev);

        put_task_struct_rcu_user(prev);
    }

    return rq;
}
```

---

## 六、地址空间切换

### 6.1 switch_mm_irqs_off() 函数

```c
// 文件: arch/x86/mm/tlb.c

void switch_mm_irqs_off(struct mm_struct *prev, struct mm_struct *next,
                        struct task_struct *tsk)
{
    struct mm_struct *real_prev = this_cpu_read(cpu_tlbstate.loaded_mm);
    u16 prev_asid = this_cpu_read(cpu_tlbstate.loaded_mm_asid);
    bool was_lazy = this_cpu_read(cpu_tlbstate_shared.is_lazy);
    unsigned cpu = smp_processor_id();
    u64 next_tlb_gen;
    bool need_flush;
    u16 new_asid;

    // 确保中断已禁用
    if (IS_ENABLED(CONFIG_PROVE_LOCKING))
        WARN_ON_ONCE(!irqs_disabled());

    // 验证 CR3 的正确性（调试模式）
#ifdef CONFIG_DEBUG_VM
    if (WARN_ON_ONCE(__read_cr3() != build_cr3(real_prev->pgd, prev_asid))) {
        __flush_tlb_all();
    }
#endif

    if (was_lazy)
        this_cpu_write(cpu_tlbstate_shared.is_lazy, false);

    // 如果切换到同一个 mm，可能只需要刷新
    if (real_prev == next) {
        // ... 懒惰切换优化 ...
        return;
    }

    // 应用安全缓解措施
    cond_mitigation(tsk);

    // 更新 CPU 掩码
    if (real_prev != &init_mm) {
        cpumask_clear_cpu(cpu, mm_cpumask(real_prev));
    }
    if (next != &init_mm)
        cpumask_set_cpu(cpu, mm_cpumask(next));
    
    // 获取 TLB 代数
    next_tlb_gen = atomic64_read(&next->context.tlb_gen);
    
    // 选择新的 ASID
    choose_new_asid(next, next_tlb_gen, &new_asid, &need_flush);

    // 标记正在切换
    this_cpu_write(cpu_tlbstate.loaded_mm, LOADED_MM_SWITCHING);
    barrier();

    // ★★★ 加载新的页表到 CR3 ★★★
    if (need_flush) {
        this_cpu_write(cpu_tlbstate.ctxs[new_asid].ctx_id, next->context.ctx_id);
        this_cpu_write(cpu_tlbstate.ctxs[new_asid].tlb_gen, next_tlb_gen);
        load_new_mm_cr3(next->pgd, new_asid, true);  // 刷新 TLB
        trace_tlb_flush(TLB_FLUSH_ON_TASK_SWITCH, TLB_FLUSH_ALL);
    } else {
        load_new_mm_cr3(next->pgd, new_asid, false); // 不刷新（使用 PCID）
        trace_tlb_flush(TLB_FLUSH_ON_TASK_SWITCH, 0);
    }

    barrier();

    // 更新状态
    this_cpu_write(cpu_tlbstate.loaded_mm, next);
    this_cpu_write(cpu_tlbstate.loaded_mm_asid, new_asid);

    if (next != real_prev) {
        cr4_update_pce_mm(next);
        switch_ldt(real_prev, next);  // 切换 LDT
    }
}
```

### 6.2 load_new_mm_cr3()

```c
// 文件: arch/x86/mm/tlb.c

static void load_new_mm_cr3(pgd_t *pgdir, u16 new_asid, bool need_flush)
{
    unsigned long new_mm_cr3;

    if (need_flush) {
        invalidate_user_asid(new_asid);
        new_mm_cr3 = build_cr3(pgdir, new_asid);
    } else {
        new_mm_cr3 = build_cr3_noflush(pgdir, new_asid);
    }

    // ★ 写入 CR3 寄存器，切换页表 ★
    write_cr3(new_mm_cr3);
}
```

---

## 七、寄存器切换（汇编层）

### 7.1 switch_to 宏

```c
// 文件: arch/x86/include/asm/switch_to.h

#define switch_to(prev, next, last)                 \
do {                                                \
    ((last) = __switch_to_asm((prev), (next)));     \
} while (0)
```

### 7.2 __switch_to_asm（x86-64）

这是上下文切换最核心的代码：

```asm
// 文件: arch/x86/entry/entry_64.S

/*
 * %rdi: prev task
 * %rsi: next task
 */
.pushsection .text, "ax"
SYM_FUNC_START(__switch_to_asm)
    /*
     * 保存 callee-saved 寄存器
     * 顺序必须与 inactive_task_frame 结构匹配
     */
    pushq   %rbp
    pushq   %rbx
    pushq   %r12
    pushq   %r13
    pushq   %r14
    pushq   %r15

    /* ★★★ 切换栈指针 - 这是上下文切换的核心！★★★ */
    movq    %rsp, TASK_threadsp(%rdi)    /* 保存 prev 的栈指针 */
    movq    TASK_threadsp(%rsi), %rsp    /* 加载 next 的栈指针 */

#ifdef CONFIG_STACKPROTECTOR
    /* 更新栈保护金丝雀 */
    movq    TASK_stack_canary(%rsi), %rbx
    movq    %rbx, PER_CPU_VAR(fixed_percpu_data) + stack_canary_offset
#endif

    /*
     * RSB (Return Stack Buffer) 填充
     * 防止推测执行攻击
     */
    FILL_RETURN_BUFFER %r12, RSB_CLEAR_LOOPS, X86_FEATURE_RSB_CTXSW

    /* 恢复 callee-saved 寄存器（从新栈弹出）*/
    popq    %r15
    popq    %r14
    popq    %r13
    popq    %r12
    popq    %rbx
    popq    %rbp

    /* 跳转到 C 函数完成剩余工作 */
    jmp     __switch_to
SYM_FUNC_END(__switch_to_asm)
.popsection
```

### 7.3 栈切换图解

```
切换前：                              切换后：
                                     
进程A的栈                             进程B的栈
┌─────────────┐                      ┌─────────────┐
│    ...      │                      │    ...      │
├─────────────┤                      ├─────────────┤
│    r15      │                      │    r15      │ ← pop
│    r14      │                      │    r14      │ ← pop
│    r13      │                      │    r13      │ ← pop
│    r12      │                      │    r12      │ ← pop
│    rbx      │                      │    rbx      │ ← pop
│    rbp      │                      │    rbp      │ ← pop
├─────────────┤ ← RSP (保存到       ├─────────────┤ ← RSP (从
│    ...      │    A->thread.sp)     │    ...      │    B->thread.sp 加载)
└─────────────┘                      └─────────────┘

1. 把寄存器压入A的栈
2. 保存A的RSP到A->thread.sp
3. 从B->thread.sp加载RSP（栈切换！）
4. 从B的栈弹出寄存器
```

---

## 八、硬件上下文切换（C层）

### 8.1 __switch_to()（x86-64）

```c
// 文件: arch/x86/kernel/process_64.c

__visible __notrace_funcgraph struct task_struct *
__switch_to(struct task_struct *prev_p, struct task_struct *next_p)
{
    struct thread_struct *prev = &prev_p->thread;
    struct thread_struct *next = &next_p->thread;
    struct fpu *prev_fpu = &prev->fpu;
    struct fpu *next_fpu = &next->fpu;
    int cpu = smp_processor_id();

    WARN_ON_ONCE(IS_ENABLED(CONFIG_DEBUG_ENTRY) &&
                 this_cpu_read(hardirq_stack_inuse));

    /* 1. 保存 FPU 状态 */
    if (!test_thread_flag(TIF_NEED_FPU_LOAD))
        switch_fpu_prepare(prev_fpu, cpu);

    /* 2. 保存 FS/GS 段寄存器 */
    save_fsgs(prev_p);

    /* 3. 加载新任务的 TLS 描述符 */
    load_TLS(next, cpu);

    /* 4. 结束懒惰模式（虚拟化相关）*/
    arch_end_context_switch(next_p);

    /* 5. 切换 DS 和 ES 段寄存器 */
    savesegment(es, prev->es);
    if (unlikely(next->es | prev->es))
        loadsegment(es, next->es);

    savesegment(ds, prev->ds);
    if (unlikely(next->ds | prev->ds))
        loadsegment(ds, next->ds);

    /* 6. 加载 FS/GS base */
    x86_fsgsbase_load(prev, next);

    /* 7. 加载 PKRU（内存保护键）*/
    x86_pkru_load(prev, next);

    /* 8. 更新 per-CPU 变量 */
    this_cpu_write(current_task, next_p);
    this_cpu_write(cpu_current_top_of_stack, task_top_of_stack(next_p));

    /* 9. 完成 FPU 切换 */
    switch_fpu_finish(next_fpu);

    /* 10. 更新内核栈指针 */
    update_task_stack(next_p);

    /* 11. 处理额外切换（I/O 权限位图等）*/
    switch_to_extra(prev_p, next_p);

    /* 12. AMD SYSRET 问题修复 */
    if (static_cpu_has_bug(X86_BUG_SYSRET_SS_ATTRS)) {
        unsigned short ss_sel;
        savesegment(ss, ss_sel);
        if (ss_sel != __KERNEL_DS)
            loadsegment(ss, __KERNEL_DS);
    }

    /* 返回 prev，用于 finish_task_switch() */
    return prev_p;
}
```

### 8.2 FPU 状态切换

```c
// 文件: arch/x86/include/asm/fpu/internal.h

/*
 * FPU 切换采用"懒惰"策略：
 * - switch_fpu_prepare() 保存旧状态
 * - switch_fpu_finish() 设置 TIF_NEED_FPU_LOAD 标志
 * - 实际加载在返回用户态或内核使用FPU时进行
 */

static inline void switch_fpu_prepare(struct fpu *old_fpu, int cpu)
{
    if (static_cpu_has(X86_FEATURE_FPU) && !(current->flags & PF_KTHREAD)) {
        // 保存 FPU 寄存器到内存
        save_fpregs_to_fpstate(old_fpu);
        
        // 记录上次运行的 CPU（用于懒惰恢复优化）
        old_fpu->last_cpu = cpu;

        trace_x86_fpu_regs_deactivated(old_fpu);
    }
}

static inline void switch_fpu_finish(struct fpu *new_fpu)
{
    // 设置标志，表示需要在返回用户态时加载 FPU
    if (cpu_feature_enabled(X86_FEATURE_FPU))
        set_thread_flag(TIF_NEED_FPU_LOAD);
}
```

---

## 九、上下文信息保存位置

### 9.1 保存位置总览

```
┌─────────────────────────────────────────────────────────────┐
│                      task_struct                            │
│  (进程描述符，每个进程一个，位于内核内存)                     │
│                                                             │
│  ├── thread_struct thread    ← 硬件上下文                   │
│  │     ├── sp (内核栈指针)    ★ 上下文切换核心              │
│  │     ├── fs/gs base                                       │
│  │     ├── TLS描述符                                        │
│  │     ├── fpu (FPU/SSE/AVX状态)                           │
│  │     └── ...                                              │
│  │                                                          │
│  └── void *stack            ← 指向内核栈                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    内核栈 (Kernel Stack)                     │
│                    大小: 16KB (x86-64)                       │
│                                                             │
│  栈顶 (高地址) ─────────────────────────────────────────────│
│  │   struct pt_regs         ← 用户态寄存器 (系统调用/中断时)│
│  │   ├── rax, rbx, rcx...                                   │
│  │   ├── rip, rsp, rflags                                   │
│  │   └── cs, ss                                             │
│  │                                                          │
│  │   ... 函数调用帧 ...                                      │
│  │                                                          │
│  │   struct inactive_task_frame  ← 上下文切换时的寄存器     │
│  │   ├── r15, r14, r13, r12                                 │
│  │   ├── rbx, rbp                                           │
│  │   └── ret_addr          ★ 切换后从这里继续执行           │
│  ▼                                                          │
│  栈底 (低地址) ─────────────────────────────────────────────│
│                                                             │
│  thread.sp ──────────────────────→ 指向 inactive_task_frame │
└─────────────────────────────────────────────────────────────┘
```

### 9.2 内核栈大小

```c
// 文件: arch/x86/include/asm/page_64_types.h

#define THREAD_SIZE_ORDER   (2 + KASAN_STACK_ORDER)
#define THREAD_SIZE         (PAGE_SIZE << THREAD_SIZE_ORDER)
// x86-64: THREAD_SIZE = 4KB × 4 = 16KB
```

### 9.3 保存内容详表

| 类别 | 保存内容 | 保存位置 | 保存时机 |
|------|----------|----------|----------|
| **CPU通用寄存器** | RAX, RBX, RCX, RDX, RSI, RDI, R8-R15 | `pt_regs` (栈上) | 进入内核时 |
| **栈指针** | RSP | `thread.sp` | 上下文切换时 |
| **帧指针** | RBP | 栈上 (inactive_task_frame) | 上下文切换时 |
| **指令指针** | RIP | `pt_regs` / 栈返回地址 | 进入内核时 |
| **标志寄存器** | RFLAGS | `pt_regs` (栈上) | 进入内核时 |
| **段寄存器** | CS, SS, DS, ES, FS, GS | `pt_regs` + `thread` | 切换时保存 |
| **FS/GS Base** | fsbase, gsbase | `thread.fsbase/gsbase` | `__switch_to()` |
| **FPU/SSE/AVX** | XMM0-15, YMM, ZMM, MXCSR | `thread.fpu.state` | `switch_fpu_prepare()` |
| **TLS描述符** | 线程本地存储 | `thread.tls_array[]` | `load_TLS()` |
| **页表基址** | CR3 | `mm_struct.pgd` | `switch_mm_irqs_off()` |
| **调试寄存器** | DR0-DR7 | `thread.ptrace_dr7` 等 | 需要时 |
| **I/O权限** | IOPL, I/O位图 | `thread.io_bitmap` | `switch_to_extra()` |
| **PKRU** | 内存保护键 | `thread.pkru` | `x86_pkru_load()` |

---

## 十、系统调用与上下文切换的区别

### 10.1 系统调用流程

```
┌────────────────────────────────────────────────────────────────────────┐
│                           系统调用流程                                  │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  用户态                              内核态                            │
│  ┌──────────┐                       ┌──────────┐                      │
│  │ 程序代码  │                       │ 内核代码  │                      │
│  │          │                       │          │                      │
│  │ printf() │                       │ sys_write│                      │
│  │    │     │                       │    │     │                      │
│  │    ▼     │                       │    ▼     │                      │
│  │ syscall  │ ─────────────────────→│ 入口代码  │                      │
│  │ 指令     │    ① 保存用户态寄存器 │          │                      │
│  │          │       到 pt_regs      │          │                      │
│  │          │                       │ 执行内核 │                      │
│  │          │                       │ 代码     │                      │
│  │          │ ←────────────────────│          │                      │
│  │ 继续执行  │    ② 恢复用户态寄存器 │ sysret   │                      │
│  │          │       从 pt_regs      │          │                      │
│  └──────────┘                       └──────────┘                      │
│                                                                        │
│  ★ 始终是同一个进程！                                                  │
│  ★ 不需要保存内核态信息（执行完就丢弃）                                 │
│  ★ 这是"模式切换"，不是"上下文切换"                                    │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### 10.2 上下文切换流程

```
┌────────────────────────────────────────────────────────────────────────┐
│                           上下文切换流程                                │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│  进程A内核态                          进程B内核态                       │
│  ┌──────────┐                        ┌──────────┐                     │
│  │ 执行中... │                        │ 之前睡眠 │                     │
│  │          │                        │ 在此处   │                     │
│  │ schedule │                        │          │                     │
│  │    │     │                        │          │                     │
│  │    ▼     │                        │          │                     │
│  │context_  │                        │          │                     │
│  │ switch   │ ─────────────────────→ │ 恢复执行 │                     │
│  │          │  ① 保存A的寄存器       │          │                     │
│  │          │  ② 保存A的栈指针       │          │                     │
│  │          │  ③ 切换页表            │          │                     │
│  │          │  ④ 加载B的栈指针       │          │                     │
│  │          │  ⑤ 恢复B的寄存器       │          │                     │
│  │  (睡眠)  │                        │ 继续执行 │                     │
│  └──────────┘                        └──────────┘                     │
│                                                                        │
│  ★ 换了一个进程！                                                      │
│  ★ 需要保存完整的上下文                                                │
│  ★ 这才是真正的"上下文切换"                                            │
│                                                                        │
└────────────────────────────────────────────────────────────────────────┘
```

### 10.3 时间线对比

```
时间 ──────────────────────────────────────────────────────────────────→

【系统调用】（无上下文切换）

进程A: [用户态] ──syscall──→ [内核态] ──sysret──→ [用户态]
                   │                        │
              保存pt_regs              恢复pt_regs
              (同一进程)               (同一进程)


【上下文切换】

进程A: [用户态] ─→ [内核态] ─→ [睡眠........................] ─→ [内核态] ─→ [用户态]
                        │                                          ↑
                   保存上下文                                  恢复上下文
                   到A的栈+thread                             从A的栈+thread
                        │                                          │
                        └──────────────────────────────────────────┘
                                          ↑
                                    (可能很长时间)

进程B:       [睡眠...] ─→ [内核态] ─→ [用户态] ─→ [内核态] ─→ [睡眠...]
                              ↑
                         从这里切入
```

### 10.4 关键区别总结

| 比较项 | 系统调用 | 上下文切换 |
|--------|----------|------------|
| **进程变化** | 同一进程 | 不同进程 |
| **特权级变化** | Ring 3 → Ring 0 → Ring 3 | 保持 Ring 0 |
| **页表切换** | 不需要 | 需要（如果进程不同） |
| **保存用户态寄存器** | 是（pt_regs） | 之前已保存 |
| **保存内核态寄存器** | 不需要 | 需要 |
| **保存栈指针** | 不需要 | 需要（thread.sp） |
| **FPU 切换** | 不需要 | 需要 |
| **TLB 影响** | 无 | 可能刷新 |
| **开销** | 较小（~100 cycles） | 较大（~1000+ cycles） |

---

## 十一、总结

### 11.1 上下文切换的本质

**上下文切换的核心是栈切换**：

```c
// __switch_to_asm 中最关键的两行：
movq  %rsp, TASK_threadsp(%rdi)   // 保存当前栈指针到 prev->thread.sp
movq  TASK_threadsp(%rsi), %rsp   // 从 next->thread.sp 加载新栈指针
```

一旦栈指针切换：
- 后续的 `pop` 从新栈取数据
- `ret` 跳转到新栈上保存的返回地址
- "魔法般"地切换到了另一个进程

### 11.2 上下文切换涉及的内容

1. **CPU 寄存器**
   - 通用寄存器 (RAX-R15)
   - 栈指针 (RSP, RBP)
   - 段寄存器 (CS, SS, DS, ES, FS, GS)
   - FS/GS Base
   - 标志寄存器 (RFLAGS)

2. **FPU/SIMD 状态**
   - x87 FPU 寄存器
   - SSE 寄存器 (XMM0-15)
   - AVX 寄存器 (YMM, ZMM)
   - MXCSR

3. **地址空间**
   - 页表 (CR3)
   - TLB（可能需要刷新）
   - LDT

4. **其他硬件状态**
   - TLS 描述符
   - I/O 权限位图
   - PKRU（内存保护键）
   - 调试寄存器

### 11.3 保存位置

| 信息类型 | 保存位置 |
|----------|----------|
| 用户态寄存器 | 内核栈顶 (`pt_regs`) |
| 内核栈指针 | `task_struct.thread.sp` |
| 内核态寄存器 | 各自的内核栈 (`inactive_task_frame`) |
| FPU/SIMD | `task_struct.thread.fpu` |
| 段寄存器 | `task_struct.thread` |
| 页表 | `mm_struct.pgd` |

### 11.4 一句话总结

> **上下文切换 = 保存当前进程的 CPU 状态到内存 + 从内存恢复另一个进程的 CPU 状态**
> 
> **核心技巧：通过切换栈指针 (RSP)，让 `ret` 指令自然跳转到新进程的执行位置**

---

## 参考文件

| 文件路径 | 内容 |
|----------|------|
| `kernel/sched/core.c` | 调度器核心，schedule(), context_switch() |
| `arch/x86/entry/entry_64.S` | __switch_to_asm 汇编实现 |
| `arch/x86/kernel/process_64.c` | __switch_to() C 函数 |
| `arch/x86/mm/tlb.c` | switch_mm_irqs_off() 页表切换 |
| `arch/x86/include/asm/switch_to.h` | switch_to 宏，inactive_task_frame |
| `arch/x86/include/asm/processor.h` | thread_struct 定义 |
| `arch/x86/include/asm/ptrace.h` | pt_regs 定义 |
| `arch/x86/include/asm/fpu/types.h` | fpu 结构定义 |
